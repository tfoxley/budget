<!DOCTYPE HTML>
<html>
    <head>
    	<title>New List</title>
    	<meta http-equiv="content-type" content="text/html; charset=utf-8" />
    	<meta name="viewport" content="width=device-width, user-scalable=no" />
    	<%= stylesheet_link_tag 'main' %>
    </head>
    <body>
    	<div id="header">
    		<h1>New List</h1>
    		<span class="left"><a href="/savings_accounts"><img src="/images/back.png" alt="back"/></a></span>
    		<span class="left-2"><a href="/"><img src="/images/home.png" alt="home"/></a></span>
    	</div>

        <% form_for(@savings_account) do |f| %>
            <% if @savings_account.errors.any? %>
                <div id="error_explanation">
                    <h2><%= pluralize(@savings_account.errors.count, "error") %> prohibited this list from being saved:</h2>

                    <ul>
                    <% @savings_account.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                    </ul>
                </div>
            <% end %>

        	<div class="form">
        		<p><%= f.text_field :name, :class => "text-field", :placeholder => :Name %></p>
        		<p><%= f.text_field :amount, :class => "text-field", :placeholder => :Amount %></p>
        		<p class="buttons">
        			<%= f.submit 'Create', :class => "button" %> <input type="button" class="button" onclick="window.location='/savings_accounts';" value="Cancel">
        		</p>
        	</div>
        <% end %>
    </body>
</html>