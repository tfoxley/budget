<!DOCTYPE HTML>
<html>
    <head>
    	<title>Edit List</title>
    	<meta http-equiv="content-type" content="text/html; charset=utf-8" />
    	<meta name="viewport" content="width=device-width, user-scalable=no" />
		<%= stylesheet_link_tag 'mobile' %>
    </head>
    <body>
    	<div id="header">
    		<h1>Edit List</h1>
    		<span class="left"><a href="/lists" class="sprite back-icon"></a></span>
    		<span class="left-2"><a href="/" class="sprite home-icon"></a></span>
    		<span class="right"><a href="/lists/new" class="sprite new-icon"></a></span>
    	</div>

        <% form_for(@list) do |f| %>
            <% if @list.errors.any? %>
                <div id="error_explanation">
                    <h2>Please fix errors:</h2>
                    <ul>
                    <% @list.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                    </ul>
                </div>
            <% end %>

        	<div class="form">
        		<p><%= f.text_field :name, :class => "text-field", :placeholder => :Name %></p>
        		<p><%= f.text_area :description, :class => "textarea", :placeholder => :Description %></p>
        		<p class="buttons">
        			<%= f.submit 'Update', :class => "button" %> <input type="button" class="button" onclick="window.location='/lists';" value="Cancel">
        		</p>
        	</div>
        <% end %>
        
        <p class="buttons delete">
    	    <%= button_to "DELETE", { :controller => :lists, :action => 'destroy' }, { :id => @list.id, :confirm => "Are you sure?", :class => "delete-button", :method => :delete } %>
    	</p>
        
    </body>
</html>