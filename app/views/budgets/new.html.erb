<!DOCTYPE HTML>
<html>
    <head>
    	<title>New Budget</title>
    	<meta http-equiv="content-type" content="text/html; charset=utf-8" />
    	<meta name="viewport" content="width=device-width, user-scalable=no" />
    	<%= stylesheet_link_tag 'main' %>
    </head>
    <body>
    	<div id="header">
    		<h1>New Budget</h1>
    		<span class="left"><a href="/budgets/<%= params[:year] %>/<%= params[:month] %>"><img src="/images/back.png" alt="back"/></a></span>
    		<span class="left-2"><a href="/"><img src="/images/home.png" alt="home"/></a></span>
    	</div>
	
    	<% form_for(@budget) do |f| %>
    	    <% if @budget.errors.any? %>
                <div id="error_explanation">
                    <h2>Please fix errors:</h2>
                    <ul>
                    <% @budget.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                    </ul>
                </div>
            <% end %>
    		<div class="form">
    			<p><%= f.text_field :amount, :class => "text-field", :placeholder => :Amount %></p>
    			<p><%= f.select :category_id, @categories, {}, :class => "select-box wide" %></p>
    			<p class="buttons">
    			    <%= hidden_field_tag(:month, params[:month]) %>
    			    <%= hidden_field_tag(:year, params[:year]) %>
    				<%= f.submit 'Create', :class => "button" %> <input type="button" class="button" onclick="window.location='/budgets/<%= params[:year] %>/<%= params[:month] %>';" value="Cancel">
    			</p>
    		</div>
    	<% end %>
    </body>
</html>