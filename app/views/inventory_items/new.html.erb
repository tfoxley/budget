<!DOCTYPE HTML>
<html>
    <head>
    	<title>Inventory</title>
    	<meta http-equiv="content-type" content="text/html; charset=utf-8" />
    	<meta name="viewport" content="width=device-width, user-scalable=no" />
    	<%= stylesheet_link_tag 'main' %>
    </head>
    <body>
    	<div id="header">
    		<h1>New Item</h1>
    		<span class="left"><a href="/inventories/<%= @inventory_item.inventory_id %>"><img src="/images/back.png" alt="back"/></a></span>
    		<span class="left-2"><a href="/"><img src="/images/home.png" alt="home"/></a></span>
    	</div>
	
    	<% form_for(@inventory_item) do |f| %>
    	    <% if @inventory_item.errors.any? %>
                <div id="error_explanation">
                    <h2><%= pluralize(@inventory_item.errors.count, "error") %> prohibited this inventory item from being saved:</h2>

                    <ul>
                    <% @inventory_item.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                    </ul>
                </div>
            <% end %>
    		<table>
    			<tr>
    				<td class="text-field-label"><%= f.label :name %></td>
    				<td><%= f.text_field :name, :class => "text-field" %></td>
    			</tr>
    			<tr>
    				<td class="text-field-label"><%= f.label :quantity %></td>
    				<td><%= f.text_field :quantity, :class => "quantity" %></td>
    			</tr>
    			<tr>
    				<td class="textarea-label"><%= f.label :description %></td>
    				<td><%= f.text_area :description, :class => "textarea" %></td>
    			</tr>
    			<tr>
    				<td class="buttons" colspan="2">
    					<%= hidden_field(:inventory_item, :inventory_id, :value => params[:inventory]) %>
    					<%= f.submit 'Add' %> <input type="button" onclick="window.location='/inventories/<%= params[:inventory] %>';" value="Cancel">
    				</td>
    			</tr>
    		</table>
    	<% end %>
    </body>
</html>