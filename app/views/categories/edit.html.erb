<!DOCTYPE HTML>
<html>
    <head>
    	<title>Edit Category</title>
    	<meta http-equiv="content-type" content="text/html; charset=utf-8" />
    	<meta name="viewport" content="width=device-width, user-scalable=no" />
		<%= stylesheet_link_tag 'mobile' %>
    </head>
    <body>
    	<div id="header">
    		<h1>Edit Category</h1>
    		<span class="left"><a href="/budgets/<%= params[:year] %>/<%= params[:month] %>" class="sprite back-icon"></a></span>
    		<span class="left-2"><a href="/" class="sprite home-icon"></a></span>
    	</div>
	
    	<% form_for(@category) do |f| %>
    	    <% if @category.errors.any? %>
                <div id="error_explanation">
                    <h2>Please fix errors:</h2>
                    <ul>
                    <% @category.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                    </ul>
                </div>
            <% end %>
    		<div class="form">
    			<p><%= f.text_field :name, :class => "text-field", :placeholder => :Name %></p>
    			<p class="buttons">
    			    <%= hidden_field_tag(:month, params[:month]) %>
    			    <%= hidden_field_tag(:year, params[:year]) %>
    				<%= f.submit 'Update', :class => "button" %> <input type="button" class="button" onclick="window.location='/categories/<%= params[:year] %>/<%= params[:month] %>';" value="Cancel">
    			</p>
    		</div>
    	<% end %>
    	
    	<p class="buttons delete">
    	    <%= button_to "DELETE", { :controller => :categories, :action => 'destroy' }, { :id => @category.id, :confirm => "Are you sure?", :class => "delete-button", :disabled => "disabled", :method => :delete } %>
    	</p>
    </body>
</html>