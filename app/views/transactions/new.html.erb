<!DOCTYPE HTML>
<html>
    <head>
    	<title>New Transaction</title>
    	<meta http-equiv="content-type" content="text/html; charset=utf-8" />
    	<meta name="viewport" content="width=device-width, user-scalable=no" />
    	<%= stylesheet_link_tag 'main' %>
    	<script>
    	    window.addEventListener("load",function() {
              setTimeout(function(){
                window.scrollTo(0, 1);
              }, 0);
            });
        </script>
    </head>
    <body>
    	<div id="header">
    		<h1>New Transaction</h1>
    		<span class="left"><a href="/transactions/<%= params[:year] %>/<%= params[:month] %>" class="sprite back-icon"></a></span>
    		<span class="left-2"><a href="/" class="sprite home-icon"></a></span>
    	</div>
	
    	<% form_for(@transaction) do |f| %>
    	    <% if @transaction.errors.any? %>
                <div id="error_explanation">
                    <h2>Please fix errors:</h2>
                    <ul>
                    <% @transaction.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                    </ul>
                </div>
            <% end %>
    		<div class="form">
    		    <p><%= f.date_select :date, {}, :class => "select-box" %></p>
    		    <p><%= f.text_field :to, :class => "text-field", :placeholder => "To (payee)" %></p>
    			<p><%= f.text_field :amount, :class => "text-field", :type => "number", :placeholder => :Amount %></p>
    			<p><%= f.select :category_id, @categories.collect {|c| [ c.name, c.id ] }, {}, :class => "select-box wide" %></p>
    			<p class="buttons">
    				<%= f.submit 'Create', :class => "button" %> <input type="button" class="button" onclick="window.location='/transactions/<%= params[:year] %>/<%= params[:month] %>';" value="Cancel">
    			</p>
    		</div>
    	<% end %>
    </body>
</html>